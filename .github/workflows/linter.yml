name: Super-Linter

on:
  pull_request:
    branches: [main]

jobs:
  super-lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Super-Linter
        uses: github/super-linter@v6
        env:
          CHECKOV_IGNORE_CHECKS: CKV2_GHA_1
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DEFAULT_BRANCH: main
